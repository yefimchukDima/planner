/**
 * Enumeration representing available application paths.
 * @enum {string}
 */
export enum PATH {
  /** Represents the home page path. */
  HOME = '/',

  /** Represents any path that's not found within the application. */
  NOT_FOUND = '*',
}
